//! interface;

//* use case

// Mostly use ):-  objects / API response / props

interface User {
  id: string;
  name: string;
  email: string;
  isActive: boolean;
}

const user: User = {
  id: "1",
  name: "Ishwar",
  email: "ishwar@gmail.com",
  isActive: true,
};
